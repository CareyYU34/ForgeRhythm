# 自適應打擊門檻說明書（PF_HIT 的「升」與「降」）

> 對應程式：`src/poseEngine/adaptiveMonitor.js`、`src/poseEngine/conditions.js`、`src/poseEngine/calibrationProfile.js`
> 這份文件說明手部打擊門檻 `PF_HIT` 如何在遊玩過程中**自動調整**：一條路徑把它「降」到你真實的打擊習慣，另一條路徑在你「一直打不中」時把它「升」到打得到。

---

## 0. 先看懂三個名詞

| 名詞 | 意思 |
|---|---|
| **PF** | 手基準點到「髖→膝」大腿線的垂直距離，除以大腿長正規化。**手越靠近大腿，PF 越小**（貼著大腿 ≈ 0.09；手抬高 ≈ 0.5～1.0）。 |
| **PF_HIT** | 打擊門檻。當 `PF ≤ PF_HIT`（且速度達標）就觸發一次打擊。**PF_HIT 越大 → 越好打**（手還沒很靠近就觸發）；越小 → 越難打。 |
| **canHit** | 遲滯狀態。命中後設為 `false`（上鎖），手抬離（`PF > PF_RELEASE`）才重新 `true`（開放下一次）。**關鍵：`canHit` 只有在「真的命中」時才會 true→false。** |
| **trough（低谷）** | 一次動作裡 PF 的最低點。 |

`PF_HIT` 的來源公式（`calibrationProfile.js`）：

```
PF_HIT = clamp( troughPF × K1 , PF_HIT_FLOOR , peakCap )
         K1 = 2.0，PF_HIT_FLOOR = 0.15，peakCap = peakPFMean × 0.6
```

也就是說 **PF_HIT 基本上由「你休息時手貼大腿的低谷 `troughPF`」乘 2 得到**。

---

## 1. 一句話總覽

`PF_HIT` 被兩條方向相反的路徑一起拉扯，自然收斂到「剛好符合你打擊習慣」的位置：

```
            ┌──────────────── 升（變好打）────────────────┐
            │  near-miss 升級器                          │
            │  「有下擊、但差一點才觸發」連續數次 → 把 PF_HIT 往上推  │
            ▼                                            │
   ┌─────────────┐                                       │
   │   PF_HIT    │◀──────────────────────────────────────┘
   └─────────────┘
            ▲
            │  降（收斂回真實位置）
            │  7 格低谷中位數估計器
            └── 每次「成功命中」把休息低谷丟進 7 格 → 中位數 × K1 → 限速拉回
```

- **降** = 你能打中時，用成功命中的休息低谷把 PF_HIT 拉回真實位置。
- **升** = 你一直打不中時，把 PF_HIT 往上推到打得到。
- 兩者作用在**同一個** `PF_HIT`，一推一拉就收斂。

---

## 2. 降路徑：**PF 低谷中位數估計器**（就是你說的「7 次」）

> 位置：`updateTroughSampling` → `closeCycle` → `emitTroughSample` → `applyPFHitUpdate`
> 程式註解裡的 **M1～M4**。

### 2.1 一個「樣本」怎麼來（M1）

不是每一幀的 PF，而是**一次「休息循環」的最低值**：

- `canHit` **true→false**（真的命中那刻，手進入大腿附近）→ **開啟**循環
- `canHit` **false→true**（手抬離大腿）→ **關閉**循環，把這段期間的**最低 PF** 產出為**一個樣本**

所以每個樣本 ≈ 「這一下打完、手安定在大腿上時的最低 PF」（≈ 0.09）。

### 2.2 7 格滾動 buffer 取中位數（M3）

- 樣本存進容量 **7** 的環形 buffer（滿了擠掉最舊的 → 跟著近況走）。
- 少於 **3** 個樣本不更新。
- 估計值 `troughPF` = **這些樣本的中位數**（排序後正中間那一個）。

**為什麼是中位數不是平均？** 抗離群值。7 個值排序取中間，兩端再怎麼極端都不影響中間：

```
樣本： 0.08, 0.11, 0.09, 0.095, 0.085, 0.10, 0.09
排序： 0.08, 0.085, 0.09, [0.09], 0.095, 0.10, 0.11   → 中位數 = 0.09
就算混進一個怪值 0.30：中位數仍然是 0.09（平均則會被拉到 0.12）
```

因為 miss 不進 buffer（見 §2.5），7 格裡全是集中在 ~0.09 的成功休息低谷，所以中位數穩穩落在真實休息位。

### 2.3 有效性過濾（M2）

`PF > 0.3`（`TROUGH_MAX_PF`）的幀**直接拒絕、不 clamp**（把超界值壓到 0.3 會製造不存在的假低谷）。一個循環需 ≥ **3** 幀有效低谷才算數。

### 2.4 變化率限制（M4）

每次更新 `PF_HIT` 最多變動 **±15%**（`PF_HIT_MAX_STEP_RATIO`）。孤立異常樣本會被自動稀釋，不需事先判斷是否異常。這也是「降」是**漸進**而非瞬間的原因。

### 2.5 為什麼「miss」進不了這個 7 格 —— 也就是本次要解的問題

循環的開啟條件是 `canHit` **true→false**，而 `canHit` **只有真的命中才會變 false**。
所以「有做打擊動作、但 PF 沒低於 PF_HIT（沒觸發）」的 miss，`canHit` 全程 true → **循環永遠不開 → 樣本一個都不產生**。

後果（死結）：若你的力道到不了 PF_HIT，就**永遠沒有樣本**、`troughPF` 不更新、`PF_HIT` 卡在打不到的位置 → 永遠打不出來。這正是 §3 要解的。

> ⚠️ **踩過的雷**：曾經試過「直接把下擊谷底丟進 7 格」。但用力拍下去那瞬間手壓到大腿線、PF 逼近 0，樣本被灌成 ~0.02 → `PF_HIT = 0.02×2 ≈ 0.05` 比手平常位置還低 → 越打越低、完全觸發不了。**教訓：餵給 `troughPF` 的必須是「休息位」，不能是「接觸最低點」，miss 也不能直接進 7 格。**

---

## 3. 升路徑：**near-miss 升級器**（本次新增）

> 位置：`updateHandNearMiss` → `raiseHitFromNearMiss`
> 目的：解開 §2.5 的死結 —— 讓「一直打不中」也能推動門檻。

### 3.1 一次下擊怎麼被「切」出來（循環邊界）

near-miss **不靠 `canHit` 循環**（那只有命中才會翻），而是用 PF 曲線的**兩個幾何邊緣**把「一次下擊」框出來：

```
PF
0.6 ┤        ╭─╮ peakPF                    ╭─╮
    │       ╱   ╲                         ╱   ╲
armLevel··╱·····╲····(PF_HIT+0.2)······╱·····╲··  ← 抬過它 = 武裝（起點）
    │    ╱       ╲                     ╱       ╲
0.2 ┤  ╱          ╲ strokeMin ╭───────╱
    │              ╲_________╱  ↑
PF_HIT┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┼┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄
    └──────────────────────────┼──────────────→ t
                     回升 >0.03 │ = 反轉確認（終點，解除武裝）
```

- **起點界線 = `PF_HIT + 0.2`（基於 PF_HIT）**：手抬過它才武裝；`!armed` 保證一次下擊只武裝一次。PF_HIT 一變，這條線跟著移動。
- **終點界線 = `本次谷底 + 0.03`（基於這次下擊自己，與 PF_HIT 無關）**：谷底回升超過 0.03 就判定「到底、回來了」→ 解除武裝。
- **下一次**：解除武裝後，手必須**再抬過 `armLevel`** 才會重新武裝 → 一次清楚的「抬起→到底→回升」= 一個循環。中途小抖動（回升 <0.03）不會提前結束，`strokeMin` 只往下更新（連續下探算同一次、取最低）。

> 對照：7 格中位數用 `canHit` 邊緣（命中/釋放）切循環，只吃得到成功；near-miss 用幾何邊緣切，miss 也切得出來。

### 3.2 怎麼偵測一次 near-miss

near-miss = 「有做下擊、谷底落在 `PF_HIT` 之上一點點（差一點就中）」。逐幀狀態機：

1. **武裝**：手抬到 `armLevel = PF_HIT + 0.2`（`HAND_NM_MAX_GAP`）之上，開始追這一次下擊（只在上升穿越時武裝一次）。
2. **追谷底**：記錄這次下擊的最高點 `peakPF` 與最低點 `strokeMin`。
3. **確認反轉**：谷底回升超過 `0.03`（`HAND_NM_REVERSAL_DELTA`）才算這次下擊結束（濾單幀跳點）。
4. **深度門檻**：`peakPF − strokeMin ≥ 0.15`（`HAND_NM_MIN_DEPTH`）才算真攻擊（濾除在 armLevel 附近懸停的抖動）。
5. **判定 near-miss**：谷底在 **`PF_HIT` 之上、且差距 `谷底 − PF_HIT ≤ 0.2`**（絕對差，與 PF_HIT 大小無關）→ 記一次 near-miss，並記住這串裡「最接近門檻的谷底」`bestMissMin`。

> 谷底 ≤ PF_HIT 的不是 near-miss（那本來就會觸發，或只是速度沒達標）；谷底離 PF_HIT 超過 0.2 的代表手根本沒下來到位，忽略。
> 例：PF_HIT = 0.1，谷底 0.2（差 0.1）→ 算；谷底 0.35（差 0.25）→ 不算。

### 3.3 什麼時候上調、上調多少

- 連續 **2** 次 near-miss（`HAND_NM_COUNT`）且距上次上調 ≥ **3 秒**（`HAND_NM_COOLDOWN_MS`）→ 上調。
- 目標：`PF_HIT = min(peakCap, bestMissMin × 1.05)`（`HAND_NM_MARGIN`）—— 略高於你最接近的谷底，讓同樣力道下一次剛好能觸發。
- **只升不降**：`newPFHit = max(現值, 目標)`。因為 `bestMissMin` 依定義 > PF_HIT，所以一定是往上。
- 成功命中一次就把 near-miss 計數清零（已能打中，交回 §2 處理）。

### 3.4 兩個關鍵約束（為什麼這樣才安全）

1. **只改 `PF_HIT` / `PF_RELEASE`，絕不寫 `troughPF`、不進 7 格。**
   讓 §2 的中位數永遠錨定在真實休息位，之後才有辦法把被升高的 PF_HIT 拉回來。（違反這條 = 重演 §2.5 的雷。）
2. **偵測到有效下擊會重置 M5 的 stuck 計時。**
   M5（故障回退）會在「`canHit` 卡在 true 超過 3.5 秒」時把 PF_HIT 退回 baseline。但 near-miss streak 期間 `canHit` 正是全程 true，若不處理，M5 會一直把升級器推高的門檻拉回去打架。所以偵測到「使用者正在下擊嘗試」就重置 M5 計時 → M5 只在「真的閒置卡住」時才動。

---

## 4. 升與降如何收斂（實測數字）

以預設右手（`PF_HIT = 0.1938`）為例，模擬「使用者下擊谷底停在 0.25（打不到）」，之後改成「能打中、休息位 0.09」：

```
一直 miss（谷底 0.25）×2   → near-miss 升級：PF_HIT 0.1938 → 0.2625   （解死結）
成功命中 ×3               → 7 格中位數 target=0.18，限速下降：       → 0.2231
成功命中 ×4               →                                        → 0.1896
成功命中 ×5               →                                        → 0.1800（收斂）
成功命中 ×6               →                                        → 0.1800（穩定）
```

- **升**把打不到的死結解開；**降**在能打中後把門檻收斂回真實位置。
- `troughPF` 全程只被真實休息樣本移動（0.0969 → 0.09），miss 的 0.25 **從未進入 7 格**。
- 回到 0.18 後穩定，不震盪。

> 註：這裡用「休息 0.09」示範降路徑。若你是快速點擊、谷底停在較高處，成功後 `troughPF` 就會反映那個較高值，PF_HIT 穩定在較高處 —— 系統依你**實際**的打擊深度自我校準。

---

## 5. 參數速查表（都在 `MONITOR_TUNING` / `TUNING`）

### 降路徑（7 格低谷中位數）
| 參數 | 值 | 意義 |
|---|---|---|
| `TROUGH_SAMPLE_SIZE` | 7 | 樣本環形 buffer 容量（就是「7 次」） |
| `TROUGH_MIN_SAMPLES` | 3 | 少於此數不更新 PF_HIT |
| `TROUGH_MAX_PF` | 0.3 | 低谷候選上界，超出直接拒絕 |
| `TROUGH_CYCLE_MIN_FRAMES` | 3 | 一循環需幾幀有效低谷 |
| `PF_HIT_MAX_STEP_RATIO` | 0.15 | 每次更新最多 ±15%（M4 限速） |
| `K1` | 2.0 | `PF_HIT = troughPF × K1` |
| `PF_HIT_FLOOR` | 0.15 | PF_HIT 絕對下限 |
| `PF_HIT_PEAK_CAP_RATIO` | 0.6 | PF_HIT 上限 = peakPFMean × 0.6 |

### 升路徑（near-miss 升級器）
| 參數 | 值 | 意義 |
|---|---|---|
| `HAND_NM_MAX_GAP` | 0.2 | 差點就中：谷底 − PF_HIT ≤ 此值才算（絕對差） |
| `HAND_NM_MIN_DEPTH` | 0.15 | 下擊需此深度才算真攻擊 |
| `HAND_NM_REVERSAL_DELTA` | 0.03 | 谷底回升超過此量才算下擊結束 |
| `HAND_NM_COUNT` | 2 | 累積幾次 near-miss 才上調 |
| `HAND_NM_COOLDOWN_MS` | 3000 | 兩次上調間冷卻 |
| `HAND_NM_MARGIN` | 1.05 | 上調目標 = 最接近谷底 × 此值 |

---

## 6. 設計禁忌（改動前必讀）

- **別把 miss 的谷底丟進 7 格中位數。** miss 谷底與休息低谷是方向相反、意義不同的兩個量，混在一起會讓 `troughPF` 亂跳。（歷史雷，見 §2.5。）
- **near-miss 升級器別去寫 `troughPF`。** 只能改 `PF_HIT` / `PF_RELEASE`。否則「降」路徑失去錨點，PF_HIT 被升上去就回不來。
- **改 near-miss 升級器時記得它與 M5 的互動。** 若拿掉「偵測下擊重置 M5 計時」，M5 會在 miss streak 把升級拉回 baseline。
- **`PF_RELEASE` 一律用 `computeReleaseFromHit(PF_HIT, peakPFMean)` 推導**，別在兩處各寫一份公式。

---

## 7. 觀察用 log（開 console 就看得到）

- 產生成功樣本：`[adaptiveMonitor] <zone> 樣本產出: 0.09xx buffer=n/7`
- 降路徑更新：`[adaptiveMonitor] <zone> 估計更新: troughPF … PF_HIT a → b（受限速截斷…）`
- 升路徑觸發：`[adaptiveMonitor] <zone> near-miss 升級 PF_HIT: a → b（最接近谷底 …）`
- M5 回退：`[adaptiveMonitor] <zone> 故障回退: canHit 卡住 …s`
